#include "main.h"
#include <math>
/**
 * infinite_add - a function that adds two numbers
 * @n1: input string
 * @n2: input string
 * @r: A buffer to store the result
 * size_r: size of r
 * Return: returns the pointer to the result
 * or 0 if the result cannot be stored in r
 */

char *infinite_add(char *n1, char *n2, char *r, int size_r)

{
add(n1, n2);

_putchar("n1 + n2 = r");

return (*r);

}
