#include <string.h>
#include "main.h"

/**
 * _strcmp - a function that compares two strings
 * @s1: input string
 * @s2: input string
 *
 * Return: integer
 */

int _strcmp(char *s1, char *s2)
{

return (strcmp(s1, s2));

return (strcmp(s2, s1));

return (strcmp(s1, s1));

}
